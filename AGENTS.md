# MetaEditor Rules
## 协作规则
- 只改用户明确要求修改的内容，不顺手重排、重命名、整理或重构。
- 先保持现有文件的顺序、排版、紧凑度和命名风格，再考虑别的优化。
- 修改文件时，默认采用最小补丁，不整文件重写。
- 注释默认用中文。
## MoonBit 代码风格
- 保持紧凑写法，不主动拉开空行。
- 状态 `let ...` 放在文件最开头。
- 类型定义放在下面，并和对应的类型操作放在一起。
- 类型标注写成 `x: T`，不要写成 `x : T`。
- 构造函数中的 `{{ ... }}` 按同一个符号风格保留，不要拆开整理。
- 除非用户明确要求，不主动把短写法改成长写法。